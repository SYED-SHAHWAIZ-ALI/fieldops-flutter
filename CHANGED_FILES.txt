FieldOps login branding update - changed files only

Replace/copy these paths into the ROOT of your existing fieldops project:

1. lib/features/auth/presentation/login_screen.dart
2. pubspec.yaml
3. assets/branding/fieldops_mark.png   (NEW FILE)

What changed:
- Replaced the generic location-pin block on Login with a professional FieldOps brand card.
- Added the FieldOps logo mark as a local bundled asset (no network dependency).
- Added FieldOps + Field Service Management lockup in a responsive navy/indigo header.
- Added an asset error fallback to the previous location icon, so login still renders safely if the asset ever fails.
- No auth logic, navigation, credentials, repositories, state, routes, tests, or business workflows were changed.
